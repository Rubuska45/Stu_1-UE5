# ============================================================
# UE5 学习仓库 .gitignore（只跟踪源码 + 配置，不传重型资源）
# 目标：仓库 <10MB，专注记录 C++ 代码成长轨迹
# ============================================================

# ---- Unreal Engine 生成的重型/可重建目录（绝不提交）----
Binaries/
DerivedDataCache/
Intermediate/
Saved/
.vs/
.vscode/

# ---- 生成的 Visual Studio 解决方案文件（UE 会自动重建）----
*.sln
*.slnx

# ---- 项目本地 AI / 智能体数据（与代码无关）----
.workbuddy/

# ---- 本地 GitHub PAT（明文令牌，绝不提交，仅本机 git 推送时读取）----
token.txt
ghp_*.txt
*.pat.txt

# ---- Content 资源（按约定不跟踪，仓库保持轻量）----
# 之后若想加入资源，删除此行并用 Git LFS 管理大文件即可。
Content/

# ---- AI 生成的 3D 资产源文件（glb/fbx/zip/预览图，体积大且可再生）----
# 导入 UE 后这些会变成 Content/ 里的 .uasset，而 Content/ 本身也不跟踪，
# 意味着这类资产在仓库里不留备份。要留档需删掉此行并改用 Git LFS。
GeneratedAssets/

# ---- 操作系统垃圾文件 ----
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store
